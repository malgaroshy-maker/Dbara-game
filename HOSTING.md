# خطة النشر — استضافة «دبارة» على Cloudflare

آخر تحديث: 15 أغسطس 2026

> **اللعبة منشورة بالفعل** على <https://dbara.malgaroshy.workers.dev/>. ما يلي
> يبقى مرجعاً لإعادة النشر ولضبط ما يحتاج ضبطاً عند كل بناء.

هذه خطة إيصال اللعبة إلى مجموعة تجريبية صغيرة تلعبها على هواتفها، وليست خطة
إطلاق عام. القرارات المتفق عليها:

| القرار | الاختيار |
| --- | --- |
| الاستضافة | Cloudflare Pages |
| طريقة النشر | دفع إلى GitHub ثم بناء تلقائي |
| الجمهور | مجموعة تجريبية صغيرة، برابط غير معلن |
| الإضافات | زر التبليغ عن خطأ + اختبارات Playwright في CI |
| خارج النطاق حالياً | نطاق خاص، تحليلات، حسابات، لوحة صدارة |

المستودع: <https://github.com/malgaroshy-maker/Dbara-game>

---

## لماذا هذه الخطة بهذا الترتيب

اللعبة تعمل، لكن ثلاثة أشياء تفسد تجربة أول مجموعة تجرّبها إن لم تُعالَج قبل
إرسال الرابط: أيقونة تثبيت ناقصة، وعامل خدمة قد يحبس اللاعب على نسخة قديمة،
ولاعب لا يعرف أن تقدّمه محفوظ على جهازه وحده. المرحلة صفر كلها من هذا النوع.

---

## المرحلة صفر — قبل إرسال الرابط ✅

**١. بيانات التطبيق والأيقونات (`public/manifest.webmanifest`)**
- تم توليد أيقونات PNG بدقة عالية بمقاسي 192 و512 بنوعيها (العادية و maskable) مع `apple-touch-icon.png` وتضمينها في الـ manifest و `index.html`.
- تم تحديث وصف التطبيق ليعكس الـ 26 محطة تراثية.

**٢. ملف `public/_headers`**
- تم تفعيل قاعدة منع التخزين المؤقت لملف `/sw.js` لضمان التحديث التلقائي الفوري لتطبيق الـ PWA عند كل نشر.

**٣. تنبيه أن التقدّم محلي**
- تم إضافة التنبيه صراحة في شاشة الإعدادات بأن التقدم محفوظ على هذا الجهاز وحده.

---

## المرحلة الأولى — المستودع والنشر

```bash
git remote add origin https://github.com/malgaroshy-maker/Dbara-game.git
git push -u origin master
```

ثم في Cloudflare Pages: **Connect to Git** ← اختيار المستودع، ثم:

| الحقل | القيمة |
| --- | --- |
| Framework preset | None |
| Build command | `npm run build` |
| Build output directory | `dist` |
| Node version | 20 أو أحدث |

### ⚠️ متغيّر بيئة مطلوب لظهور زر التبليغ

قناة التبليغ تأتي من بيئة البناء، **ويفشل غيابها مغلقاً**: إن لم يُضبط أي منهما
لا يظهر رابط «بلّغ عن خطأ» في اللعبة أصلاً. هذا مقصود — الفشل المغلق أفضل من
نشر رقم شخصي افتراضياً — لكنه يعني أن أول بناء بعد هذا التغيير سيُسقط الزر ما
لم يُضبط المتغيّر في إعدادات البناء على Cloudflare:

| المتغيّر | متى يُستعمل |
| --- | --- |
| `VITE_REPORT_URL` | **المفضّل.** رابط نموذج. `{subject}` يُستبدل بمعرّف السؤال. لا يكشف شيئاً شخصياً. |
| `VITE_REPORT_WHATSAPP` | بديل. رقم دولي بلا `+` ولا مسافات. **ينشر رقماً شخصياً لكل زائر.** |

كل ما في هذه المتغيّرات يُدمج في الجافاسكربت الذي يُنزّله المتصفح، فلا يوضع
فيها مفتاح ولا سر بحال. التفصيل في [`.env.example`](.env.example).

كل دفعة إلى `master` تُبنى وتُنشر، ولكل طلب دمج رابط معاينة مستقل.

**ميزة قائمة بالفعل:** أمر البناء يبدأ بـ `questions:check`، فأي خلل في بنك
الأسئلة يُفشل النشر بدل أن يصل إلى اللاعبين. لا تُزل هذا الشرط من `build`.

الناتج: رابط على هيئة `dbara-game.pages.dev`.

**حصر الوصول (اختياري):** بما أن المجموعة صغيرة، يمكن وضع الرابط خلف
Cloudflare Access بقائمة بريد مسموح بها — مجاني حتى خمسين مستخدماً — إن لم يكن
مطلوباً أن يكون الرابط مفتوحاً لمن يصله.

---

## المرحلة الثانية — التبليغ عن الأخطاء ✅

منفّذة. في `src/data/credits.ts` واجهة واحدة تقرأ القناة من بيئة البناء، ومنها:

- زر في شاشة الإعدادات: «بلّغ عن خطأ أو اقترح سؤالاً».
- رابط أسفل «معلومة ع الماشي» بعد كل سؤال: «تشك في هذه المعلومة؟ بلّغنا».

الرابط الثاني هو المهم: الرسالة تحمل **معرّف السؤال** تلقائياً. تبليغ يقول
«أحد أسئلة الأكل كان غلطاً» يكلّف ساعة بحث، وتبليغ يقول `food_trad_09` يكلّف
دقيقة.

ولم يعد هذا سداً لثغرة في التوثيق: كل سؤال صعب أو خبير صار موثّقاً بمصدر مسمّى
(167 سؤالاً موثّقاً، ولا واحد ناقص). التبليغ الآن لما لا يمسكه فحص ولا مصدر —
صياغة مثل تختلف روايتها بين المناطق، أو تفصيلة يعرفها أهل المدينة وحدهم.

---

## المرحلة الثالثة — اختبارات Playwright في CI ✅

منفّذة ومدمجة في سير عمل تلقائي `.github/workflows/ci.yml`:

- **جناح اختبارات E2E متكامل (`tests/e2e.spec.ts`)**:
  - فحص الإقلاع النظيف وخلو الطرفية من الأخطاء.
  - فحص مسار تهيئة المستخدم الجديد (Onboarding).
  - فحص الخريطة التفاعلية: النقر على المدن بدقة والتحكم في التكبير (Zoom & Pan) واستعادة المقياس.
  - فحص ميكانيكا الأسئلة وخيارات الإجابة وظهور بطاقة المعلومة ورابط الإبلاغ المزود بـ `questionId`.
  - فحص شاشة التحدي اليومي وسلسلة الأيام.
  - فحص نافذة الإعدادات وإلغائها عبر Escape.
- **التكامل في CI**: تشغيل `questions:check` + `map:audit` + `tsc` + `build` + `test:e2e` تلقائياً عند كل push/PR.

خطوة البناء في CI تضبط `VITE_REPORT_URL` على عنوان صوري (`example.invalid`) حتى
يستطيع جناح الاختبارات التحقق من رابط الإبلاغ. وهو **ليس** جهة الاتصال الحقيقية
عمداً: سجلّات CI عامة، ولا ينبغي أن يُبلغ منها شيء شخصي.

---

## المرحلة الرابعة — الإتاحة ✅

منفّذة، وأكثرها كان إصلاح ادعاءات قائمة لا إضافة ميزات:

- **النوافذ تحتجز التركيز فعلاً.** أربع نوافذ كانت تعلن `aria-modal="true"`
  بينما يخرج Tab منها إلى الخريطة خلفها، فيشتغل لاعب لوحة المفاتيح على أزرار
  لا يراها. `useModalA11y` يدخل التركيز ويدوّره ويغلق بـ Escape ويعيده لمن فتح.
- **خيارات الإجابة تُقرأ صحيحة.** كان حرف الترتيب يلتصق بنص الإجابة، فيصل
  «أ» و«حرب القرم» إلى قارئ الشاشة كلمة واحدة: «أحرب القرم».
- **تكبير الصفحة أُعيد.** قفل المقياس على واجهة أكثرها نص عربي يضرّ من يحتاج
  التكبير أكثر من غيره، وتكبير الخريطة بإصبعين لم يكن معتمداً عليه أصلاً.
- **تفضيل تقليل الحركة صار نافذاً.** كانت قاعدة CSS وحدها، وهي لا تمسّ
  Framer Motion لأنه يكتب أنماطاً مضمّنة إطاراً بإطار — وأربعة عشر مكوّناً
  تتحرك به. `MotionConfig reducedMotion="user"` في الجذر هو ما جعلها نافذة.

**ما بقي متروكاً عمداً:** دبابيس الخريطة الصغيرة (أزرار كاملة تُعرض بنصف
مقياسها عمداً وتكبر بالتقريب، ويستثنيها معيار WCAG 2.5.8 لأن موضعها هو
المعلومة)، ومؤقّت السؤال (استثناء «التوقيت الجوهري» يغطي مسابقة سرعة).

---

## ما لا تغطيه هذه الخطة

- **لا تحليلات:** لن تعرف ما يلعبه الناس إلا مما يخبرونك به. مقبول لمجموعة
  صغيرة، ويستحق المراجعة قبل أي إطلاق عام.
- **لا نطاق خاص.**
- **لا حسابات ولا لوحة صدارة حقيقية:** لا خادمة، فلا مزامنة بين الأجهزة.
- **لا سياسة خصوصية:** غير لازمة ما دام لا يُجمع شيء، ولازمة يوم تُضاف
  التحليلات أو الحسابات.

---

## ملاحظة على رقم التبليغ ✅ عولجت

كان الرقم مكتوباً في `src/data/credits.ts`، فيصير مقروءاً لكل من يفتح ملفات
اللعبة في المتصفح — وسرية المستودع لا تغيّر ذلك، لأن ما يُدمج في الحزمة يصل إلى
كل زائر بعد النشر.

الآن الرقم خارج الشفرة كلها ويأتي من بيئة البناء، والتطبيق **يفشل مغلقاً**: بلا
قناة مضبوطة لا يعرض رابط إبلاغ بدل الرجوع إلى رقم شخصي. وتُحقّق من ذلك ببناء
الحالتين: مع تعطيل المتغيّر لا يبقى في `dist` لا الرقم ولا حتى اسم مضيف
`wa.me`، لأن Vite يحذف الفرع كله كشفرة ميتة.

**ما يبقى قرارك:** ما دام `VITE_REPORT_WHATSAPP` هو المضبوط، فالرقم منشور لكل
زائر — بيئة البناء تُخرجه من `git` لا من المتصفح. إخفاؤه فعلياً يحتاج
`VITE_REPORT_URL` بنموذج (Google Form أو Tally)، وهو ما يحسن فعله قبل أي توسيع
للجمهور.
